# Item 19: type assertion

Uma type assertion verifica se um valor armazenado em uma interface possui um tipo específico.

A forma `nome, ok := valor.(string)` evita pânico: `ok` informa se a conversão foi possível.

## Executar

```powershell
go run main.go
```
