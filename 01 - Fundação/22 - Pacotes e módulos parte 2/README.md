# Item 22: pacotes e módulos, parte 2

O exemplo importa o pacote padrão `strings` e usa `ToUpper` para transformar texto.

Um módulo Go é inicializado com `go mod init nome-do-modulo` e registra suas dependências em `go.mod`.

## Executar

```powershell
go run main.go
```
